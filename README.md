How to run:
Navigate to the main folder (the one that contains /src /include ) and run the make command.

After the alpha.exe file is produced run as cmd arg the test file.
For example: ./alpha.exe tests/test1.txt

